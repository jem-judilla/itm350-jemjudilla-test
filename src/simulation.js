function runSimulation(input) {
  if (input === null || input < 0) {
    return false; // Error case not covered by the test
  }
  // Rest of the simulation logic
  return true;
}

module.exports = runSimulation;